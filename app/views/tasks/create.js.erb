alert("success");
// $('#calendar').css("background-color", "red");
var eventSources= [ // put the array in the `events` property
                {
                    title  : 'event3',
                    start  : '2012-12-11 12:30:00',
					end		:'2012-12-11 14:30:00',
					allDay : false
                }
    ];
alert(eventSources);
$('#calendar').fullCalendar( 'addEventSource', eventSources );
alert("end");
