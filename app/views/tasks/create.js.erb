// 添加新规的task到calendar上面
var eventSources= [ // put the array in the `events` property
                {
                    title  : 'event3',
                     start  : "<%= DateTime.new(Date.today.year, Date.today.month, Date.today.day, 12, 30, 0)%>",
                     end    : "<%= DateTime.new(Date.today.year, Date.today.month, Date.today.day, 14, 30, 0)%>",
					allDay : false
                }
    ];
$('#calendar').fullCalendar( 'addEventSource', eventSources );

$('#dataform').html(
    "<%= escape_javascript( render :partial => 'detail_task') %>"
    );
